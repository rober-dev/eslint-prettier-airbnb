const msg = 'hola mundo';

var x = 3;

for (var x = 0; x < 100; x++) {
  console.log('Esto es una prueba para verificar el uso de Prettier');
}

let app;
app.get('/', function(req, res) {
  res.send('Hello World!');
});
